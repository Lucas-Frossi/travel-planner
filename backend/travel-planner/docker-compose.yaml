services:
  mysql:
    image: mysql:8.0
    container_name: travel-planner-mysql
    environment:
      MYSQL_ROOT_PASSWORD: REMOVED_SECRET
      MYSQL_DATABASE: travel_planner
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: